# Example env file for EquanixWebsite
# Copy this to .env (or set these env vars in your system) and replace with your Supabase values.

# Supabase project URL and anon key (for client-side auth)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# Optional: existing backend URL if you have one (fallback)
REACT_APP_API_URL=http://localhost:5001/api

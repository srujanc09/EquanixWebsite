# Environment Configuration (example)
# Copy this file to backend/.env and fill in secrets. Do NOT commit backend/.env

NODE_ENV=development
PORT=5001

# Database
MONGODB_URI=mongodb://localhost:27017/equanix_trading

# JWT Secret Keys (replace with secure random strings)
JWT_SECRET=replace_with_random_string
JWT_REFRESH_SECRET=replace_with_random_string

# JWT Expiration Times
JWT_EXPIRE=24h
JWT_REFRESH_EXPIRE=7d

# CORS Settings
CLIENT_URL=http://localhost:3000

# File Upload Settings
MAX_FILE_SIZE=5242880

# Email Configuration (for future features) - fill or remove
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# API Keys (for trading data integration)
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key
FINNHUB_API_KEY=your_finnhub_key

# Supabase (optional - recommended for cross-device auth)
# SUPABASE_URL should be your project URL (e.g. https://xyz.supabase.co)
# SUPABASE_SERVICE_ROLE_KEY is the secret service role key (server-only)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME
